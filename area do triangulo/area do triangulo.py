import func

base = float(input('digite a base do triangulo '))
altura = float(input('digite a altura do triangulo '))


print('')

resultado_final = func.resultado(base,altura)

print( 'O cálculo da área do triângulo é: ',resultado_final)